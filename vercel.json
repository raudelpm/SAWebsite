{
  "buildCommand": null,
  "outputDirectory": "Website",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/Website/$1" }
  ]
}
